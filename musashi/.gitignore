# Rolling network configuration — re-pin with ./pin-config.sh, never commit.
config/
# Chain DB, Leios SQLite stores, socket, logs.
data/
# Captured operational logs kept beside the runbook.
*.log
*.log.gz
# Python runtime artifacts from local analysis.
__pycache__/
*.py[cod]
# Plaintext block-producer credentials. The encrypted keys.tar.asc archive is intentionally tracked.
keys/
