# Journal — Phase 0 (Scope Discovery)

Reverse-chronological log. Newest date sections first; newest entries first within a date. See [AGENTS.md](../AGENTS.md) § Repository Blueprint for the format rules.

## 2026-09-21

### Generated the SPO credentials, and matched the docs to the stated threat model ⏳🤖

Brian's point that these are ephemeral testnet keys — never reused on mainnet, preprod, or preview, regenerated when the network is respun — removed the reason I had written a script instead of just making the keys, so I ran it: `musashi/keys/` now holds the cold key and counter, VRF, KES, BLS, payment, and stake keys, with both addresses. Pool id `pool13pssq9ar0n7t7jt3y7cmusl06ufhd02j7wacwuanmnursyyr7qu`. The op-cert is not issued — that step needs the tip from a running node, which is not reachable from here.

Also trimmed the key-handling ceremony from [the procedure](../musashi/block-producer.md) and the script's comments, and replaced it with an explicit note on *why* it is absent, so a later reader does not re-add cold-key ritual to a throwaway pool — or, worse, copy this section's habits to one that matters. Two precautions stayed, and the note says plainly that neither is about secrecy: the node **refuses to start** when the VRF signing key is group- or world-readable (a functional check, `checkVRFFilePermissions`), and the script still refuses to overwrite an existing key set, because a new cold key means re-registering the pool and re-requesting the faucet delegation.

### Scripted the SPO credential and registration flow ⏳🤖

Brian asked for the SPO keys or a script; a script is the right artifact — he owns the secrets, it is re-runnable, and it can refuse to do the dangerous thing. Wrote [`musashi/make-spo-keys.sh`](../musashi/make-spo-keys.sh) (five key pairs, both addresses, the op-cert) and [`musashi/register-pool.sh`](../musashi/register-pool.sh) (the three certificates, the transaction, and submission only on request). Both locate `cardano-cli` via `$CARDANO_CLI` → `./build` searched recursively → `PATH`, and read the network magic, `slotsPerKESPeriod`, `minPoolCost`, `poolDeposit` and `keyDeposit` from the pinned `config/` rather than hard-coding any of them.

The safety properties are the point. `make-spo-keys.sh` **refuses to overwrite existing credentials** — a regenerated cold key is a different pool — and names the `mv` command instead; it separates `keys` / `opcert` / `show`, where `opcert` doubles as the KES-rotation step and is the only part needing the node. `register-pool.sh` demands `RELAY_HOST` or `RELAY_IPV4` rather than defaulting them (on a single-node setup this node *is* the registered relay), refuses a `POOL_COST` below the network's `minPoolCost`, refuses a UTxO too small for the deposits, and submits only when told to.

Both were **run** with the w36 CLI extracted from the image, in a scratch directory: 15 credential files with the right permissions, a real pool id, both addresses, the overwrite guard firing on a second run, and all three certificates building. Decoding the pool certificate's CBOR confirmed the Leios registration end to end — after the pool and VRF hashes sits a `list(2)` of a **96-byte** and a **48-byte** bytestring, exactly a BLS12-381 min-sig public key (G2) and its possession proof (G1). That claim had been resting on ledger types; it now rests on an encoded certificate.

Brian also ruled the SPO Rewards Program out as inappropriate for an IOG-run pool, so the guide now says so and the scripts add no metadata to the registration transaction.

### Found the testnet's operator documentation, which answers the funding question ⏳🤖

Brian asked for the Discord link for requesting stake, and whether there is an SPO faucet. Both exist, and finding them corrected a claim I had made twice: **musashi's funding route is documented** — not on the operations book, but on the project's own site, `site/docs/testnet/` in the repo (`getting-started.md`, `register-stake-pool.md`, `rewards-program.md`), published at leios.cardano-scaling.org and linked from the repo README. I had searched the ops book and the repo's implementer-facing `testnet/` directory, concluded "ask the operators", and put it on the to-do list; it was self-service all along.

What is actually there: the [faucet](https://faucet.leios.play.dev.cardano.org/basic-faucet) sends a fixed **10,000 test ada** and its *delegate* widget delegates **~1,000,000 test ada** to a bech32 pool id — so funds and stake are both self-service; support is the **Musashi Dōjō Discord**; and there is an **SPO Rewards Program** whose application form issues an **Application Code** that must ride in the pool-registration transaction's *metadata*, which means **applying before registering** or paying for a second certificate. The program pays to a mainnet address, probes registered relays with `cardano-ping`, and — notably for us — explicitly rewards **sharing node logs**, on the argument that the chain records outcomes while the timing lives in the nodes. That is the same case [the instrumentation note](../artifacts/leios-tx-flow-instrumentation.md) makes, which makes collection worth more than internal convenience.

Two corrections to our own records came out of it. My "budget a few GB" disk estimate in the cheatsheet is replaced by upstream's stated **~25 GB SSD**, 2 cores, 4 GB RAM. And a finding worth sending upstream: their Docker section documents exactly our pattern — `pin-config.sh` plus a read-only `/app/config` mount — but says the mount "is optional — drop it to fall back to the in-image snapshot", with an example pinning `prototype-2026w30`. Both put a node on the wrong chain instance today, which is the failure we spent this morning diagnosing.

[`musashi/block-producer.md`](../musashi/block-producer.md) now opens by pointing at upstream's guide as canonical, carries the faucet figures and the apply-first ordering, and gains the two host prerequisites I had omitted: a public port (our single node *is* the registered relay, so the probe hits it) and an accurate clock.

### Wrote the single-node block-producer procedure ⏳🤖

Brian wants the relay itself to forge — one node doing both jobs, deliberately non-standard. Wrote [musashi/block-producer.md](../musashi/block-producer.md) and [musashi/musashi-bp.yaml](../musashi/musashi-bp.yaml): five key pairs (the fifth, BLS, exists only in the Dijkstra era), the certificates, the submission, the pod swap, the verification queries, and the rotations. Every command shape was read from the **w36 CLI** extracted from the running image rather than from memory, and every flag name checked against its `--help` — `--key-reg-deposit-amt`, `stake-delegation-certificate --cold-verification-key-file`, `query kes-period-info --op-cert-file`, and the rest.

Three things worth recording beyond the recipe. **The blocker is funding, not configuration**: musashi charges a 500 ₳ pool deposit and a 2 ₳ stake-key deposit, `--pool-cost` has a 170 ₳ floor, and a pool with zero active stake is seated in no committee — so the whole procedure stops at "ask the operators for funds", which is already a standing to-do. **The two Leios-specific rotations coincide at ≈93 days** — KES (62 evolutions × 36 h) and the BLS key (`maxKeyAge` 374 six-hour epochs) — but renewing the BLS key means *re-registering the pool*, since the key rides on the pool certificate rather than on a reissuable certificate of its own; a pool whose key has aged out holds a seat it cannot vote from, and any certificate bit set on it invalidates that certificate. And **timing is fast here**: 6-hour epochs mean eligibility and committee seating 12–18 hours after submission, not days.

Also checked a claim before shipping it: the node validates file permissions on the **VRF** key only (`checkVRFFilePermissions`), not KES, so the usual "chmod 600 or it won't start" advice is narrower than folklore has it.

The relay spec's commented producer sketch is replaced by a pointer to the real pod, so there is one source of truth; both specs pin the same image digest and the comments in each say to bump them together.

### Worked out how to collect transaction-flow data, and found the arrival path silenced ⏳🤖

Brian asked how to collect data on transaction flow — push, pull, mempool, cache — from the running relay. Wrote [artifacts/leios-tx-flow-instrumentation.md](../artifacts/leios-tx-flow-instrumentation.md). The method that made it cheap: `cardano-node trace-documentation --config config.json` run with the **w36 binary itself** against our pinned config, which emits the full namespace list (650), the metrics help (143), and — per namespace — whether a trace is `Visible` or `Invisible` **under that config**. That turns "which traces will I actually get" from source reading into one command, and it is config-aware, so the answers are about this deployment rather than about defaults.

**The finding that changes the plan: the deployed config silences the entire transaction-arrival path.** Every `TxSubmission.TxInbound.*`, `Remote.*`, `Local.*` and `TxOutbound.*` namespace sits at `Debug`/`Info` under a `Notice` filter and is reported Invisible; `Mempool.AttemptAdd` and `Mempool.SyncNotNeeded` are explicitly `Silence`d, so the mempool accept-rate *denominator* is off while `AddedTx`/`RejectedTx` are on. Meanwhile the four Leios namespaces run at `Debug` with no rate limit. So push and pull data need a config edit and a restart, not just analysis — the note carries the `jq` patch and the volume warnings (`TxSubmission.Remote.*` is the loudest family on the node).

**The good news is on the Leios side: `Consensus.LeiosKernel.BodyHits` already instruments per-EB provenance.** It fires once per EB body inserted into the TxCache, deliberately deferred until after the mempool pull, carrying `txsInEb`, `tracked`, `acquired`, `validated`, `mempoolHits`, `missedBoth`, `cacheTxCount`, `cacheLoad`, with a documented nesting contract. Cache share, mempool share, and pull share per EB therefore need **no new instrumentation** — only collection — which shortens candidate workstream (a) again. It is per EB and never per transaction, so which transactions came from where still needs either the `ebTxs` join or `tx-firehose --color`.

Also closed a standing to-do: **no `leios*` metric is registered in the documented metric set of either build**, so `leios_logmetrics_*` does not exist here; the only metric names the Leios code declares at all are the eight `leiosFetch{Body,Txs}*Bytes` counters and the `leiosDb*` gauges. Whether even those reach the Prometheus endpoint is one `curl` away on a running node, and the note says so rather than guessing.

### Ran down the `BlockPointMissing` warnings: an acquisition counter in a warning's clothing ⏳🤖

With the relay near the tip, Brian asked about frequent `BlockPointMissing` warnings. They are expected and benign, for a reason worth writing down: at [`LeiosDemoLogic.hs:958`](https://github.com/IntersectMBO/ouroboros-consensus/blob/b56977baae0740f563060a8a9171c78be865b357/ouroboros-consensus/src/ouroboros-consensus/LeiosDemoLogic.hs#L958) the trace is emitted **unconditionally** inside the novel-EB-body branch, one line before an idempotent `leiosDbInsertEbPoint` — nothing tests whether the point is actually present. So it fires once per novel, not-too-old EB body, and its rate should track `LeiosBlockAcquired` (traced a few lines later in the same block) one-for-one; it merely carries `LSWarning` severity where `BlockAcquired` carries `LSInfo`, which is what makes it conspicuous. Verified at both consensus `b56977b` and `7abeda65` — the rev the w36 build now running was pinned to — with identical code at the same line.

The FIXME above it explains the intent ("the point MUST already be present here … announcement handling inserts it"), and checking that premise turned up the more interesting fact: **`leiosDbInsertEbPoint` has exactly one production call site — that one.** The announcement path inserts into the LeiosTxCache, not the LeiosDb, so the `ebs` table is populated solely on body acquisition. Two more stale comments join the collection: the function's own haddock says it is "Called on the announcement path (forge issuing an EB, peer receiving an announcement)", and `leiosDbInsertEbBody`'s says the point "MUST already have been inserted via `leiosDbInsertEbPoint` (announcement path)".

This also qualifies the EB-overlap SQL recipe from yesterday: `ebs`/`ebTxs` sample the EBs whose **bodies this node fetched**, not the EBs it was told about, so the denominator is fetch-dependent as well as diffusion-dependent. Noted in [the tooling note](../artifacts/leios-node-tooling.md) § 3.2, with an "expected, not a fault" row added to [the cheatsheet](../musashi/cheatsheet.md)'s trace table and the finding in [facts.md](../facts.md).

### Diagnosed a stalled relay: the newest weekly image is not the network's ⏳🤖

Brian's relay stopped advancing. The log (1,000 lines, 57 seconds) is almost entirely peer churn — promote, handshake, drop, demote to cold, repeat — with the cause underneath it: **34 `ChainSync.Client.Exception`s in 54 seconds, every one the same block** at slot 854,400, the same transaction `ca42eb12…`, the same PlutusV4 script `603320d0…`, and `ValidationTagMismatch Phase2Valid (FailedUnexpectedly (PlutusFailure …))` — the producer marked the transaction phase-2 valid, our node's evaluation said the script failed. Our node rejected the block, dropped the peer that offered it, reconnected to another, and rejected it again. One `BlockchainTime.CurrentSlotUnknown` marks the chain falling behind its own era horizon.

`cardano-cli ping --tip` settled who was wrong: all five bootstrap relays were at slot 1,268,908, **~4.8 days past** the rejected block. The network accepted it; we were alone. The cause is version skew we introduced ourselves on 2026-09-21 by pinning the image to the *newest* weekly tag: `prototype-2026w38` is built from `cardano-node` `648fc48b`, whose `cabal.project` moved **cardano-ledger from `1587f21a` to `daa38fec`** relative to w36 (`afa091b4`) — and w36 is what the chain was deployed from, per the live `MinNodeVersion` and the w36 image's build date of 2026-09-07, the day of `systemStart`. On a prototype branch the ledger changes without a hard fork, so a newer node can reject a chain the network built. Both builds report `cardano-node 11.1.0.164`, so `--version` does not discriminate, and `MinNodeVersion` is a floor, not a match — it gave false comfort at pinning time.

Fixes applied: [`musashi-relay.yaml`](../musashi/musashi-relay.yaml) now pins **w36** by digest with the reasoning in a comment and the w38 line kept for when the network rolls; the [cheatsheet](../musashi/cheatsheet.md) gains an IMPORTANT box on matching the week, a troubleshooting row keyed to the exact log signature, and the `nix build` recipe for the CLI; [the tooling note](../artifacts/leios-node-tooling.md) gains § 2.7 with the three commands that settle a skew question.

Two by-products of opening both images. The `dijkstra node`, `query`, and `stake-pool` command surfaces are **byte-identical** between w36 and w38, so the BLS findings hold for the build now running — but the **Leios parameter-update flags exist only on w38**, so parameter-variation experiments against musashi wait on the network rolling forward, while a local devnet does not. And a correction to yesterday's tooling note, which stated the nine flags as available without qualification.

### Surveyed the tooling available around a running node ⏳🤖

Brian has the relay syncing, so the question became what to point at it. Wrote [artifacts/leios-node-tooling.md](../artifacts/leios-node-tooling.md), and took an approach worth repeating: rather than read the CLI's source, I pulled the two binaries out of the published image's layers and **ran them**, so every command list, flag, and error message in the document is the output of the exact build now running. They are statically linked, which also means `podman cp` yields a Leios-capable `cardano-cli` for any x86-64 host — and they report git rev `648fc48b`, the head of `cardano-node`'s `leios-prototype` branch, five days ahead of our documented `7e33674` pin.

**What exists.** `cardano-cli dijkstra` carries a complete Leios surface except for queries: `node key-gen-BLS` / `key-hash-BLS` for the voting key; `stake-pool registration-certificate` with a **mandatory** `--bls-signing-key-file` (the signing key, since the certificate carries a proof of possession — stricter than the ledger's own `StrictMaybe`); and all nine Leios parameters in `governance action create-protocol-parameters-update`. `ping` gave real numbers against the testnet — N2N V_14–V_16, `perasSupport = PerasUnsupported`, ~30 ms — and `debug check-node-configuration` proved, on deliberately corrupted copies, that it checks a config's internal consistency and **cannot** detect the wrong network, so it would pass the stale baked config that started this whole thread. Beside the node sit two Leios-motivated tools aimed squarely at ARC #83's subject: `tx-firehose`, whose `--color` tags transactions in metadata label 1022, and its per-node `mempool-monitor` observer. `demo/dozen-devnet` (three producers × three relays, fully meshed) is the rehearsal ground for block production, BLS registration, and parameter changes without musashi stake.

**The find worth acting on.** The LeiosDb is plain SQLite in WAL mode with `ebs` and `ebTxs` tables, so **EB overlap — kleioscan's "tx duplication" — is a SQL query on our own relay**, no instrumentation needed, subject to it being one node's GC-truncated and diffusion-dependent sample. That shortens candidate workstream (a) considerably.

**The gaps, stated because they bound any single-relay result.** `dijkstra query` has 29 subcommands and not one is Leios-specific; a source scan finds no `GetLeios…` local-state query either, so this is a missing protocol surface rather than a missing CLI wrapper. Votes and certificates are the least observable part of the system: in-memory only, absent from the LeiosDb, and dropped by `trace-translator`, which recognizes Praos plus EB events and ignores the rest. Mempool capacity is reported in one byte dimension while a validation-time dimension binds first on the prototype — upstream's own `mempool-monitor` README says so, which sharpens the same point in [the metrics mapping](../artifacts/leios-mempool-metrics-mapping.md).

Also corrected a claim recorded earlier today in [facts.md](../facts.md): the bootstrap relay's IPv6 addresses were never verified — this sandbox has no IPv6 route, so the eight `Network unreachable` warnings `ping` printed are a fact about the sandbox, not the testnet.

### Set up a podman node environment for the musashi testnet ⏳🤖

Created [`musashi/`](../musashi/): a `podman kube` pod spec, a config-pinning script, and a [cheatsheet](../musashi/cheatsheet.md) for running a non-block-producing relay on the Leios prototype testnet. The node itself is upstream's published image (`ghcr.io/input-output-hk/ouroboros-leios/cardano-node-testnet`, weekly `prototype-2026wNN` tags, digest-pinned in the pod spec), so this adds orchestration rather than a build.

The substantive finding is why the setup cannot just run that image as-is: **the image's baked configuration is for a different chain instance than the network now running.** `ouroboros-leios`' committed `testnet/config/` snapshot has `systemStart` 2026-08-07 and `MinNodeVersion` w32; the configuration published at `environments-pre/leios` has `systemStart` 2026-09-07 and `MinNodeVersion` w36. Same network magic (164), different chain — a node started on the baked copy computes slot times from the wrong epoch and follows nothing. Upstream intends the snapshot to be re-pinned and has not since the network rolled. So [`pin-config.sh`](../musashi/pin-config.sh) fetches the eight published files, validates them as JSON, records their pre-patch SHA-256 sums, prints the parameters in force, and the pod mounts the result read-only over `/app/config`; `config/`, `data/`, and `keys/` are gitignored rather than committed, which is the same trap seen from the other side.

Two by-products. First, a re-verification: today's `config.json`, `dijkstra-genesis.json`, and `shelley-genesis.json` are **byte-identical** to the 2026-09-17 read, so the 14-slot certification gap stands on the current week's parameters, and our "weekly redeploys" caveat overstates the churn — the chain instance has been stable a fortnight while the *images* roll weekly. Second, the ground work for the SPO step: `cardano-node` takes `--shelley-bls-key` for the Leios voting key, and the corresponding public key plus proof of possession ride on the **pool registration certificate** (`sppBlsKey` on `StakePoolParams`), with the ledger stamping `bksRegisteredIn` — the epoch the 374-epoch `maxKeyAge` expiry counts from. How to obtain musashi stake is not established and is queued as an ask. Also two more unenforceable-by-convenience lessons in [meta-lessons-learned.md](../meta-lessons-learned.md), one of them a fabricated commit hash caught before shipping.

The pod was **not** started here — this sandbox forbids the nested user namespace podman needs — so the YAML is verified against podman 5.4.1's documented field support and the image's own metadata, not against a running node. The cheatsheet says so at the top.

## 2026-09-18

### Recorded the work stream this effort falls under ⏳🤖

Noted in the [README](../README.md) that this work falls under [`input-output-hk/ARC-operating-model` #83](https://github.com/input-output-hk/ARC-operating-model/issues/83) — “[CBU] - Leios: mempool fragmentation TBD”, labeled **Stream** (the label's own gloss: an itemized memo idea sized to fit in three months), assigned to Brian W. Bush and Will Wolff, open as of today. Recorded as a line in the README's metadata header and expanded in its hosting-and-absences section. This **supersedes the “there remains no ticket board” clause** in the correction callout on the housekeeping entry below: there is a single stream-level issue on the ARC operating-model board, though still no per-task board — findings, artifacts, and workstream candidates continue to live in this repository's records rather than as tickets. The issue's title also names a narrower subject (mempool fragmentation) than the exploration has covered, which is worth reconciling at the 2026-09-21 scope brainstorm, whose other named participant is the same co-assignee.

## 2026-09-17

### Brought the standing records up to date ⏳🤖

Housekeeping sweep across the repository-level records. Added a [README.md](../README.md) — the repository had none: a human entry point giving the current phase and its gate, a question-indexed map of the nine artifacts, the layout, how to read the pinned-snapshot and provenance/scrutiny conventions, and the deliberate absence of a remote and a board. Updated [AGENTS.md](../AGENTS.md) on three stale points: the cheatsheet is no longer "to be started" (first populated 2026-09-17), the timeline now records the 2026-09-21 scope brainstorm as its one fixed point, and the blueprint names the README. Cross-referenced the production-side document set both ways: the [catalog](../artifacts/leios-simulation-model-catalog.md) and [staging-branch snapshot](../artifacts/cardano-node-status.md) now point at the mempool/TxCache map, the parameter note, and both diagrams, so no document in the set is reachable only by knowing it exists. Corrected the cheatsheet's own status note, which still listed Protocol Parameters as a stub after it was populated. Verified with no changes needed: [meta-lessons-learned.md](../meta-lessons-learned.md) (six entries, all carrying provenance lines), [facts.md](../facts.md), and this journal's ordering.

> [!WARNING]
> Correction, 2026-09-17: the README has nine question-indexed rows linking eleven distinct records, not nine artifacts. The repository is also no longer remote-free: its standalone history is temporarily published as the unrelated `bwbush/tmp-explorations` branch of `ouroboros-leios`. There remains no ticket board.

### Reviewed Codex's corrections 🤖

Went through the external review pass Codex applied to the parameter note, timeline, cheatsheet, facts, and journal, re-verifying each load-bearing claim against the pinned clones rather than accepting it. **Nine substantive corrections confirmed and kept:** the reference-count arithmetic is 2,777 and 13,888, not 2,776/13,887; `maxEndorserBlockReferencesSize` is only a *count proxy* (the measure has no reference-bytes coordinate, and real 37-byte entries overrun the 36-byte minimum the conversion assumes), so the honest forge enforces three EB coordinates exactly and approximates the fourth; `computeMempoolCapacity` rounds an override up to whole block measures, so musashi's 2,000,000 B request becomes 23 × 90,112 = 2,072,576 B (CIP target 2,180,224 B); announcement age is *not* telemetry-only — `maxAnnouncementAgeSend = 300 s` gates relay and `maxAnnouncementAgeRecv = 600 s` rejects, though neither compares with L_hdr; announcement validation is deliberately relaxed (VRF and KES/opcert checked in full, OCERT counter upper bound skipped, a too-small or unknown counter becoming `StaleOCIN` — accepted but not processed or relayed); Δ_hdr targets ~95% of honest stake, which the CIP glosses as "practical network-wide availability"; Δ_EB^W is a 15–20 s range whose 20 s end *fails* the security inequality; "three vote conditions unenforced" is better stated as two absent and one enforced incidentally outside voting; and the CIP is now pinned at `d07a30b` (commit verified to exist, and its CIP-0164 is byte-identical to the copy we read).

**One correction rejected:** Codex expanded wFA^LS as "weighted fractional active-slot sortition", which appears nowhere in the CIP — the acronym is *weighted Fait Accompli with Local Sortition* (CIP-0164's own gloss). Fixed in [facts.md](../facts.md), the [parameter note](../artifacts/leios-node-protocol-parameters.md) (§§ 3, 6) and the [cheatsheet](../artifacts/leios-cheatsheet.md), keeping the acronym searchable. Also finished the CIP pinning Codex started (seven remaining `master` links in the [mempool/TxCache map](../artifacts/leios-node-mempool-txcache.md)) and restored the timeline's subtitle framing alongside its acronym expansions. Spelling "SCRUTINY" out in the SVG instead of the ❓🤖 glyphs is an improvement worth keeping: the emoji render as tofu boxes in non-browser rasterizers.

### Diagrammed the timing inequalities, and found two divergences from the CIP 🤖

Drew [artifacts/leios-timing-inequalities.svg](../artifacts/leios-timing-inequalities.svg): a slot-indexed timeline of every timing rule governing EB voting and certification, in three tiers — the CIP-0164 design inequalities over the network characteristics (Δ_hdr, Δ_EB^O, Δ_EB^W, Δ_RB, Δ_reapply, Δ_applyTxs), the deployed period parameters composing the 14-slot gap, and an eleven-row enforcement table keyed to CIP-0164's own numbered protocol flow. Boxes are coded by enforcement status rather than by subsystem, which is the part the CIP's Figure 4 cannot show.

Checking the CIP's rules one at a time (rather than reading the timing code on its own terms) surfaced two findings, both now in [facts.md](../facts.md) and § 7 of [the parameter note](../artifacts/leios-node-protocol-parameters.md): (1) **the forge's gap guard is one slot stricter than the CIP** — `elapsed ≤ minGap ⇒ Nothing` rejects the CIP's own boundary slot, which also means the stranding window is `gap` slots (≈ 51.2%) rather than `gap − 1` (≈ 48.7%), and incidentally that the CIP's "≈51%" is a *survival* probability where ours is a *skip* probability; (2) **three of the CIP's six vote conditions are unenforced** — header-arrival-within-L_hdr is telemetry only, equivocation is detected and traced but never consulted by `goVote`, and the non-empty-EB rule is honored only incidentally by an `error` labeled "programmer error" that an empty peer EB can reach (dropping that peer). A fourth gap sits outside the CIP's list: received votes get no timing check, and `LeiosVote` carries no slot or timestamp for one to be expressible.

> [!WARNING]
> Correction, 2026-09-17: two vote conditions are absent from the voter; the nonempty-EB condition is enforced incidentally during database ingestion, not absent from the prototype. Header arrival is not checked against L_hdr for voting, but announcement age is not telemetry-only: separate five-minute relay and ten-minute receive guards apply. The CIP's ≈51% is specifically the probability of no intervening RB, hence survival under that model.

### Mapped the protocol parameters and admission inequalities 🤖

Wrote [artifacts/leios-node-protocol-parameters.md](../artifacts/leios-node-protocol-parameters.md): which parameters and inequalities decide whether blocks, votes, and certificates are created and accepted, read from source at the node's exact build pins (consensus `b56977b`, plus newly cloned cardano-ledger `1587f21` and cardano-base `fbfb3f0`). Key findings: the full Leios parameter set is nine governable Dijkstra ledger parameters (L_hdr/L_vote/L_diff in milliseconds, N_c, τ, and four EB-capacity caps); the committee is the ledger-seated top-N_c-by-stake truncation of CIP-0164 — **wFA^LS is explicitly the earlier-drafts scheme**, so our prior "wFA^LS" and "sortition" annotations were stale and are corrected (diagram, cheatsheet, map); quorum is `Σ seatWeight ≥ τ` over share-of-active-stake weights, and certificate verification (`verifyLeiosCert`: bitfield, keyless-seat rejection, weight, aggregate BLS) is **the one fully-enforced acceptance rule** — every other Byzantine-facing check (gap, EB caps, cert exclusivity, announced size) is a FIXME/TODO (`Forker.hs:618`; `Praos.hs:526` lists five unimplemented header checks), while the honest-forge side enforces them all. Deployed musashi values fetched from the live configs: τ = 0.75, N_c = 900, periods 1000/4000/7000 ms ⇒ **gap = 14 slots, conflicting with the Aug-2026 Slack claim of 10** (unreconciled ❓🤖 — flagged in the map, metrics note, and cheatsheet), plus a 2 MB mempool-capacity override. Also populated facts.md § Protocol Parameters and the cheatsheet's Protocol Parameters section, and recorded two more stale in-code comments (Validate.hs's "size bound already folded in"; selectCommitteeByStake's haddock).

> [!WARNING]
> Correction, 2026-09-17: the honest forge does not enforce all four EB limits as exact inequalities. It precisely enforces transaction bytes, execution units, and reference-script bytes, but converts the reference-list byte parameter to a minimum-entry-size count ceiling; that ceiling does not guarantee the actual encoded list fits. The configured 2,000,000 B mempool override is also rounded to a 2,072,576 B effective byte coordinate.

### Second review pass and record cleanup 🤖

The re-review of the lifecycle revisions accepted the technical fixes but flagged six residuals, all now applied: the vote-timing label is anchored to the announced slot's onset (not closure completion); the map's "relay is prompt" / "header rules only" wording caught up with the corrected diagram; CIP quorum values and ImpactAnalysis's 12&#8211;36 h estimate are now explicitly design-tier lines rather than mixed with implementation facts; capacity says "default, byte-overridable" and the ticket is repository-qualified; the overstated first author response is amended with a [re-review addendum](../artifacts/leios-node-tx-lifecycle-review.md); and the record conventions are squared &#8212; a dated `> [!WARNING]` on the frozen freshest-first claim below, provenance lines on the new meta-lessons, the audit's implementation facts in [facts.md](../facts.md), and the first population of [the cheatsheet](../artifacts/leios-cheatsheet.md) (11 entries under Leios Mechanisms), which the maintenance rule had owed since 2026-09-16.

### Applied the lifecycle-diagram accuracy review 🤖

Worked through [the accuracy review](../artifacts/leios-node-tx-lifecycle-review.md) of the lifecycle diagram, re-verifying its findings against the clone at `b56977b` (11 spot-checks, all confirmed) before applying them. The load-bearing corrections: the skipped-EB causality was backwards (the minimum certification gap *forbids* certification, so a successor RB elected inside it strands the EB; the 1 − 0.95^g figure is a model under f = 0.05, now marked ❓🤖); a CertRB's body is not empty (no regular txs; it carries the certificate — `mkBody`); missing-closure CertRB parking is *landed*, not design intent (`ignoreUnacquiredCertRB`, `chainSelReprocessLeiosEb`); and the gap is forge policy only — follower validation carries a `FIXME` (`Forker.hs:618`), making a conformance property a natural ask. Smaller fixes: two-tier fetch priority (fresh window oldest-first), `PeerState`/`ElState` vs `ElBimap` roles, 64 KiB job cap, the −1 notified sentinel, 3 s/4 s vote-timing stubs, non-linear LeiosDb status paths with 3→1 promotion rescue, producer-vs-follower mempool behavior for skipped-EB txs, and two stale source comments the map had trusted (ChainSync folding *is* wired). Diagram (24 edits), map (§§ 1.3, 4.2, 4.3, 4.5, 4.6, 7.1), and the published artifact all updated; author response appended to the review.

### Source-verified the lifecycle diagram's flows 🤖

Audited every operation in [the lifecycle diagram](../artifacts/leios-node-tx-lifecycle.svg) against the pinned clone (`b56977b`), answering whether it depends on design documents. Mechanisms and function names all trace to source; two of my annotations did not survive: (1) mempool `Sync` revalidation is bounded by `implSyncWithLedger`'s converging off-lock loop (`syncDeltaCap = 256`, `syncMaxIters = 8` — Update.hs), not by `reapplyUntilTimeout`, which bounds snapshot computation (`computeSnapshot`, Query.hs); (2) serve-when-secured is two-level — `MsgLeiosBlockOffer` fires on body acquisition, only `MsgLeiosBlockTxsOffer` waits for closure completion (LeiosDb `AcquiredEb`/`AcquiredEbTxs` notifications pumped per-peer in NodeToNode.hs). Both corrected in the diagram and in [the map](../artifacts/leios-node-mempool-txcache.md) (§§ 4.1, 4.3), which also gains a § 1.3 nuance: the sync-side half of the design doc's converging-delta idea is already landed. Remaining non-source annotations are now layer-attributed in the diagram (CIP values τ = 0.75, L_hdr, L_vote; the Slack-sourced 10-slot musashi gap; the modeled skip rate; ImpactAnalysis's 12–36 h closure liveness). Also confirmed in source: freshest-first fetch priority (freshest-*last* when syncing — LeiosDemoLogic.hs:358).

> [!WARNING]
> Correction (2026-09-17): "freshest-first fetch priority" above is oversimplified. `fetchPriorityTiers` is two-tiered: EBs within L = 3·L_hdr + L_vote + L_diff of the current slot are requested **oldest-first**, staler EBs then freshest-first; with the slot unknown (syncing), everything falls into the single oldest-first tier. See the accuracy-review entries above.

### Diagrammed the prototype transaction lifecycle 🤖

Drew [artifacts/leios-node-tx-lifecycle.svg](../artifacts/leios-node-tx-lifecycle.svg), a six-stage SVG of a transaction's path through the `leios-prototype` node at the pinned 2026-09-16 snapshot: submission/admission, the forge's `decideLeiosCertify` TxRB/CertRB split, diffusion at a following node (`processLeiosBlock` with the DB-before-index invariant, mempool pull, and the telemetry funnel), voting via `validateEbClosure`, CertRB certification/adoption, and drainage/eviction/GC end states. Boxes are color-coded by store (Mempool / LeiosTxCache / LeiosDb / network / forge / chain); every label is a function or type permalinked in [the mempool/TxCache map](../artifacts/leios-node-mempool-txcache.md), which now links the diagram.

## 2026-09-16

### Wrote the mempool-alignment quantity mapping 🤖

[artifacts/leios-mempool-metrics-mapping.md](../artifacts/leios-mempool-metrics-mapping.md): a master table correlating the kleioscan chain-derived fragmentation metrics, the node's TxCache-funnel telemetry, and the model quantities (π₁, overlapBytes, hypergeometric overlap, p_eb), with the caveats that break naive equalities (counts vs bytes; π₁'s Praos-block proxy measurement vs the funnel's direct EB-time measurement; vantage asymmetry; the 128-announcement cache window; skip-rate baselines at gap 10 vs 14) and a concrete four-check reconciliation experiment. Key finding from reading `pi1_derivation.md`: π₁ ≈ 0.06 was measured as "tx not seen in mempool before its Praos block" on 2025 pre-testnet data — the funnel's `mempoolHits` is the direct measurement of the same quantity, so comparing them is the first real test of π₁.

### Completed the mempool/TxCache map's task queue 🤖

Worked the seven queued tasks in [artifacts/leios-node-mempool-txcache.md](../artifacts/leios-node-mempool-txcache.md): the Optimized TxCache internals (SipHash-2-4 open addressing, backward-shift deletion, refcount+state packed in one Word64, equivalence + bounds-checked test gates); announcements/equivocation (ElBimap, 0/1/2-per-election bound, protocol-level-only validation) and the JobRootHash-committed fetch-job pool; the vote-to-certificate path (eager cert assembly at quorum, no vote GC yet, 10-slot musashi cert gap vs CIP 14); the optimistic-mempool branches (stale June probe with an explicit accepts-bad-blocks FIXME — not the double-buffered mempool); PR #2280 (open, updated today; found an EB tx-serialization bug); Giorgos's partial-vote document (maximally-conflicting-mempool attack where "a tx-cache here does not help"; EB_c/EB_o two-level certificates) and the kleioscan fragmentation metrics; and a curated 71-hit TODO/FIXME worry-list, headed by the peer-poisonable EB size check and demo-constant fetch limits.

### Mapped the production mempool and LeiosTxCache 🤖

Read the `leios-prototype` source (consensus `b56977b`, node `7e33674`) and wrote [artifacts/leios-node-mempool-txcache.md](../artifacts/leios-node-mempool-txcache.md): the three-store architecture (Mempool + LeiosTxCache index + LeiosDb), the Leios mempool changes (`getLeiosTxIndex`, `getSnapshotForNoCache`, `reapplyUntilTimeout`), the TxCache handle/index with its DB-pinning invariants and eviction ordering contract, the LeiosDb SQLite schema with volatile/immutable GC, and the four flows (EB arrival with mempool pull and the telemetry funnel; voting via reapply-on-cache-hit; CertRB closure splicing; forge-side RB/EB split with cert-branch rebase). Cross-read against the design document (double-buffered mempool designed, benchmarked, not landed), ImpactAnalysis requirement codes (implemented TxCache diverges from NEW-LeiosTxCache's LRU design), CIP-0164 normative sections, and the live Aug–Sep Slack debate on mempool fragmentation. Task queue for the remainder recorded in the document.

### Mapped the Leios production staging branches 🤖

Located where Leios production code is staged, starting from `cardano-node` and following its build pins: one `leios-prototype` branch per IntersectMBO repo (node, consensus, network, ledger, api, cli, base), moving in lockstep with the node's `cabal.project` pinning exact dependency branch HEADs; cardano-db-sync deviates with weekly `leios-wNN` branches; deployment config on cardano-playground `leios-red-team` and the "musashi" testnet environment. Recorded as a dated, fast-moving-work snapshot in [artifacts/cardano-node-status.md](../artifacts/cardano-node-status.md) (cross-referenced from the catalog) and as facts in [facts.md](../facts.md). Notable for the mempool study: `bladyjoker/leios-prototype/optimistic-mempool` staging branches exist on both node and consensus.

### Completed source-level deep dives for all twelve catalog rows 🤖

Iterated row-by-row through the [simulation/model catalog](../artifacts/leios-simulation-model-catalog.md), reading each artifact's source code, documentation, and results, and writing a commit-pinned dossier per row in [artifacts/leios-simulation-model-catalog/](../artifacts/leios-simulation-model-catalog/) (reviewed individually before each catalog fold-in). Per direction, every dossier carries a standing mempool-and-transaction-caches section, and the catalog gained a dedicated mempool/TX-cache comparison table. Highlights: sim-rs models TCP by default and is mid-migration to a shared consensus core; the Haskell simulator owns the `cleanup-policies` residence mechanism; the AUEB "in-house simulator" resolved to PeerNet and 2021-era reports; smol world is a purpose-built mempool-alignment model (blocking admission, EB-cache write-back, promotion-walk ratchet) atop CUBIC-with-loss transport; the Mininet bed contributes a BDP-probing fetch scheduler (oracle-stubbed); net-rs/Piranha realize EB-closure write-back live via `TxBodyResolver`; the ΔQ family contains a mempool/TxCache Markov model with measured miss rate π₁ ≈ 0.06 (and Kuhn's Rust ΔQ tool is recorded as a dead end, per B. Bush); `linleios` composes with ΔQ via p_eb; the post-CIP triad (hypergeometric theory / TS simulator / mainnet measurements) mutually validates; the Agda spec puts the mempool below the conformance floor; and the devnets already export LeiosTxCache-funnel and mempool-hit telemetry — the measured counterpart of every model's alignment quantity. The catalog's cross-validation and gaps sections were rewritten accordingly; the two sharpest next experiments are a three-way CUBIC transport calibration and a mempool-alignment reconciliation across rows 5/8b/10/12.

### Adversarial fact-check of the simulation catalog 🤖

An Opus subagent adversarially verified the [simulation/model catalog](../artifacts/leios-simulation-model-catalog.md) against its sources (GitHub repos/PRs, Slack threads) without repeating the discovery sweep. It confirmed most entries verbatim (Mininet test bed, leios-peernet, formal spec, post-CIP, Piranha's Slack-sourced characterization) and found substantive errors, now corrected: sim-rs *does* model TCP (a congestion-window port of the Haskell `ModelTCP.hs`, on by default; only loss/retransmission/delayed-ACKs absent) — the original "no TCP dynamics" claim was wrong; "smol world" is a draft PR with zero review activity but models full Linear Leios EB diffusion and certification, the opposite of the catalog's guess; the "unlocated" Hauser DeltaQ work is the `analysis/deltaq/linear-leios` artifact itself; `leios-peernet` is private; the pseudo-mainnet is 2,685 nodes in the current v4 (10,000 was v1); two links pointed at wrong branches/repos. Lesson for [meta-lessons-learned.md](../meta-lessons-learned.md): summary-table cells silently dropped hedges that the detail sections carried.

### Cataloged Leios simulations and models 🤖

Created [the catalog of Leios simulations and models](../artifacts/leios-simulation-model-catalog.md), answering the [#team-leios question](https://input-output-rnd.slack.com/archives/C074AHSKJF7/p1789558993289409?thread_ts=1789558993.289409&cid=C074AHSKJF7) about available network simulators, their faithfulness, status, scope, and performance. Sources swept: GitHub (five Leios repos plus two open PRs), Slack (#team-leios, #formal-methods, #high-fives, DMs), Confluence (Innovation, IOE, NC spaces), and Google Drive (simulation-analysis decks, tutorial recording, SOW). Twelve primary artifacts cataloged — seven simulators/emulators, five model/verification families — plus supporting topologies, calibration data, and a cross-validation record. Five gaps recorded, the largest being that no unified same-topology/same-workload faithfulness comparison across simulators exists. Confirmed facts written to [facts.md](../facts.md). The AUEB simulator code (Spyros Voulgaris) and the private `leios-adversarial-tools` repository remain unobtained; several claims in the catalog carry ❓🤖 SCRUTINY markers accordingly.
